{
	"name": "sst-bun-monorepo",
	"version": "0.0.0",
	"workspaces": {
		"packages": [
			"packages/*"
		],
		"catalog": {
			"@biomejs/biome": "2.3.4",
			"@tsconfig/bun": "^1",
			"@types/bun": "^1.3.1",
			"typescript": "^5"
		}
	},
	"scripts": {
		"all:build": "bun --filter '*' build",
		"all:clean": "bun --filter '*' clean",
		"all:lint": "bun --filter '*' lint",
		"all:types": "bun --filter '*' types",
		"aws:sso": "aws sso login --sso-session=fire-emblem",
		"aws:check": "aws sts get-caller-identity --profile=fe-prd",
		"lint": "biome check --write && bun --filter '*' lint",
		"deploy:prd": "bun all:lint && bun all:types && bun all:clean && bun all:build && bun sst:deploy:prd",
		"remove:prd": "sst remove --stage production",
		"sst:deploy:prd": "sst deploy --stage production",
		"test": "bun --filter '*' test"
	},
	"dependencies": {
		"sst": "^3"
	},
	"engines": {
		"bun": "1.3.1"
	},
	"packageManager": "bun@1.3.1"
}
